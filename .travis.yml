language: python
python:
  - "2.7"

notifications:
  email:
    on_failure: change # [always|never|change] default: always

before_install:
  # add for profile
  - sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test
  - sudo apt-get update -qq


install:
  - pip install git+git://github.com/ioam/param.git
  - pip install git+git://github.com/ioam/dataviews.git
  - pip install jinja2 tornado pyzmq matplotlib pillow
  - if [[ $TRAVIS_PYTHON_VERSION == 2* ]]; then pip install ipython==1.0; fi
  - if [[ $TRAVIS_PYTHON_VERSION == 3* ]]; then pip install ipython; fi
# add for profile
  - sudo apt-get install -qq g++-4.8
  - export CXX="g++-4.8"
  - pip install nose-cprof
  - pip install properform

before-script:
  - "echo 'backend : Agg' > $HOME/.matplotlib/matplotlibrc"

script:
  - nosetests --with-doctest --with-cprofile --cprofile-stats-erase --cprofile-stats-file=profile_imagen_82706c8ce9_3
  - python -m properform Push 47.88.50.110:15554 asdf_imagen_82706c8ce9_3 ./profile_imagen_82706c8ce9_3

 #2017-08-11 15:43:00 modified at 0 #2017-08-11 15:43:06 modified at 1 #2017-08-11 15:43:11 modified at 2 #2017-08-11 15:43:17 modified at 3